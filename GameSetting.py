import pygame, Screen, Characters, Paused, Start

pygame.init()

#instances of a class
screen = Screen()
start = Start()
#mainPlayer = Characters() -> don't forget the parameters x, y, width, height and speed
paused = Paused()

